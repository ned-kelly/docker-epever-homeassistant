{
  "name": "lechacal-homeassistant",
  "version": "1.0.0",
  "description": "Simple application designed to read the serial data from the Lechacal CT Clamp, and then transmit data to your HomeAssistant server via MQTT.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config-yml": "^0.10.3",
    "forever": "^3.0.2",
    "mqtt": "^4.2.0",
    "serialport": "^9.0.1"
  }
}
